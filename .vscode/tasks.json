{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo-build-release",
            "type": "shell",
            "command": "cargo",
            "args": ["build", "--release", "--no-default-features"]
        }
    ]
}